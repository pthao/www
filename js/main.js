// $(document).ready(function(){
	// $('[data-role=page]').on('pageshow', function (event, ui) { 
		// alert("Load was performed.");
		// $("#" + event.target.id).find("[data-role=footer]").load("inc/footer.html", function(){
			// $("#" + event.target.id).find("[data-role=navbar]").navbar();
			
		// });
	// });	 
	
	// //$('.footer').load("inc/footer.html");  
// });   

// $(document).bind("pageinit", function() {
    // $('.app-footer').load("inc/footer.html", function() {
        // $(this).trigger('create');
    // });
// });

// $('[data-role=page]').on('pageshow', function (event, ui) {
	// $("#" + event.target.id).find("[data-role=footer]").load("inc/footer.html", function(){
		// $("#" + event.target.id).find("[data-role=navbar]").navbar();
	// });
// });

// $(document).bind("pageinit", function() {
    // $('[data-role=page]').on('pageshow', function (event, ui) {
		// //alert(event.target.id+" - Find Footer: "+$("#" + event.target.id).find("[data-role=footer]"));
		// $("#" + event.target.id).find("[data-role=footer]").load("inc/footer.html", function(){
			// //alert("Initializing...");	//Some how it only intialize the first page. After clicking and go to the next page it stop working.
			// $("#" + event.target.id).find("[data-role=navbar]").navbar();
		// }); 
	// });
	
	// //$('#appFooter').load('inc/footer.html').trigger("create");
	// //$("#appFooter").load('inc/footer.html', function(){$(this).trigger("create")});
// });